import Button from "@mui/material/Button";

function Query() {
  return (
      <div>
        <br/><br/>
        <h2>Query</h2>
        <Button variant="contained">
          Copy from template
        </Button>
        &nbsp;&nbsp;&nbsp;&nbsp;
        <Button variant="contained">
          Save as template
        </Button>
      </div>
  );
}

export default Query;
